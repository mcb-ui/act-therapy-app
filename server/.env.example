# Copy this file to server/.env and adjust values as needed.
# The development defaults let you run the stack without extra setup.

# Port the Express API listens on. Keep 5001 to match the Vite proxy in the client.
PORT=5001

# Secret used to sign JWTs. Replace this with a secure random string before deploying.
JWT_SECRET=change-me-in-production
